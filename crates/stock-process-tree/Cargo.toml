[package]
name = "calen-stock-process-tree"
version = "0.1.0"
edition = "2021"
publish = false

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_Security", "Win32_System_JobObjects", "Win32_System_Threading"] }

[dev-dependencies]
wait-timeout = "0.2.1"
