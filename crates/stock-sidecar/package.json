{
  "name": "@calen/stock-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "calen-stock-sidecar": "dist/stdio.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node dist/stdio.mjs",
    "test": "node --test --experimental-strip-types test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "verify:bundle": "node scripts/verify-bundle.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "0.28.1",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "unpdf": "1.6.2"
  }
}
